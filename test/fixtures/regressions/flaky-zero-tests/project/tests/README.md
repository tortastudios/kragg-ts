# tests

`kragg.json` points `test_paths` at this directory, and the suite that used to
live here is gone — the files were renamed, moved, or never written. Nothing
here matches the discovery patterns, so a rerun sweep runs a command that
executes zero tests. Zero failures out of zero tests is arithmetic, not
evidence of stability.
